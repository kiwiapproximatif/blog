import { Center, Container, Divider, Text, VStack } from "@chakra-ui/react"
import Link from "./link"

const Header = () => {
    return (
        <VStack my={4}>
            <Text mb={2} fontSize="2xl">Maxence Lambard</Text>
        </VStack>
    )
}

export default Header